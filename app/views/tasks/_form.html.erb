<%= form_for @task do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <%= f.label :details %>
  <%= f.text_field :details %>
  <% if !@task.new_record? %>
    <%= f.label :Completed %>
    <%= f.check_box :completed %>
  <%end%>
  <%= f.submit %>
<% end %>
